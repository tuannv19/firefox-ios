# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Client' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RedAntSDK', :git => 'https://github.com/toffs-dev/M88-SSClient-iOS-SDK.git'
  # Pods for Client
	
  target 'ClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
target 'Tunnel' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RedAntSDK', :git => 'https://github.com/toffs-dev/M88-SSClient-iOS-SDK.git'
  # Pods for Tunnel

end
